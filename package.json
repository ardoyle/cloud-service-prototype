{
  "name": "cloud-services-prototype",
  "description": "A test project to get used to node.  Will have an endpoint to add new cloud formation templates to  mongo, as well as an endpoint to create a new ec2 instance, and yet another to get a status on that instance.",
  "version": "0.0.1",
  "author": "ADoyle",
  "main": "src/main.js",
  "dependencies": {
    "bunyan": "0.22.3",
    "mongoose": "3.8.10",
    "nock": "^0.32.3",
    "rekuire": "^0.1.5",
    "restify": "2.8.1"
  },
  "devDependencies": {
    "istanbul": "^0.2.10",
    "mocha": "1.19.0",
    "mockgoose": "1.10.0",
    "sinon": "^1.10.0",
    "supertest": "^0.13.0"
  },
  "engines": {
    "node": "0.10.x"
  },
  "scripts": {
    "test-coverage": "node_modules/.bin/istanbul cover node_modules/mocha/bin/_mocha"
  }
}
